# Parse a single R script for file dependencies

Parse a single R script for file dependencies

## Usage

``` r
parse_script(script_path)
```

## Arguments

- script_path:

  Absolute path to the R script

## Value

List with 'inputs', 'outputs', and 'externals' character vectors
