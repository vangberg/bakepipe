# Find all R scripts in project

Returns a vector with paths to all .R scripts in or below the project
root. The project root is determined by bakepipe::root().

## Usage

``` r
scripts()
```

## Value

Character vector of absolute paths to .R files
